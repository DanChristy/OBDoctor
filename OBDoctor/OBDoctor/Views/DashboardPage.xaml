<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:OBDoctor.ViewModels"
             xmlns:controls="clr-namespace:OBDoctor.Controls"
             x:Class="OBDoctor.Views.DashboardPage"
             BackgroundColor="Black">
    <ContentPage.BindingContext>
        <viewmodels:DashboardViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout
            Orientation="Horizontal" 
            HorizontalOptions="CenterAndExpand">

            <controls:RadialGauge
                BindingContext="{Binding SpeedRadialGauge}"/>
            <controls:RadialGauge
                BindingContext="{Binding RPMRadialGauge}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>